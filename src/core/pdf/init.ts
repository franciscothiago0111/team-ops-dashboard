/**
 * Initialize PDF templates on app startup
 * This should be imported in the root layout
 */

import '@/core/pdf/templates';

export function initializePDF() {
  // PDF templates are auto-registered on import
  // This function can be used for additional initialization if needed
}
